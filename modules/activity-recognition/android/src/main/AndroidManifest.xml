<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Runtime permission on Android 10+ (auto-granted on older versions). -->
  <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
  <!-- Pre-Android-10 Google Play Services equivalent. -->
  <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />

  <application>
    <!-- Receives ActivityRecognition broadcasts fired by our PendingIntent.
         Not exported — the PendingIntent itself grants the system permission
         to invoke it. -->
    <receiver
      android:name="expo.modules.activityrecognition.ActivityRecognitionReceiver"
      android:exported="false">
      <intent-filter>
        <action android:name="expo.modules.activityrecognition.ACTION_ACTIVITY_RESULT" />
      </intent-filter>
    </receiver>
  </application>
</manifest>
