node_modules/
.env
.env.backup-*
.env.local
.expo/
# Local-only editor / Claude Code settings — never tracked.
.claude/settings.local.json
eli-bridge.code-workspace
# Expo prebuild outputs at the repo root — intentionally regeneratable.
# Do NOT use `android/` unanchored; it would also match modules/*/android/
# which contains our HAND-WRITTEN Kotlin source for custom Expo modules.
/android/
/ios/
dist/
services/geminiPrompt.generated.ts

# Gradle build outputs for custom native modules — ignore build/.cxx only,
# NOT the whole android/ tree.
modules/*/android/build/
modules/*/android/.cxx/
modules/*/android/.gradle/

# Device diagnostics pulled off the Pixel. The bugreport dumps are ~145MB of
# dumpstate binaries and filesystem snapshots — useful to read, ruinous to commit
# (git history is forever, and there's no easy way back out).
Bug Report/
crash.log

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli